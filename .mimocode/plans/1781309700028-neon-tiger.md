# Sprint 3 Implementation Plan — Quality Dashboard

## Current State
- `quality.tsx` uses **mock data** from `@/lib/mock` — no API calls
- Backend `QualityController.php` is **fully implemented** with 8 endpoints (kpis, brChart, defectChart, qpTeams, alerts, annualTrend, paretoRft, paretoInspection)
- Routes exist in `web.php` under `auth` middleware
- Existing `api.ts` points to `/api/novacity` (Novacity proxy) — needs new backend-facing functions
- Cron scheduler already configured in `routes/console.php` with DB-driven intervals via `SyncSetting`
- Admin page exists but lacks sync config panel for backend intervals

---

## Changes

### 1. Create Backend-Facing Quality API Service
**File:** `resources/js/services/qualityApi.ts` (NEW)

Create dedicated fetch functions calling Laravel backend quality endpoints:
- `fetchQualityKpis()` → `GET /quality/kpis`
- `fetchQualityBrChart()` → `GET /quality/br-chart`
- `fetchQualityDefectChart()` → `GET /quality/defect-chart`
- `fetchQualityQpTeams()` → `GET /quality/qp-teams`
- `fetchQualityAlerts()` → `GET /quality/alerts`
- `fetchQualityAnnualTrend()` → `GET /quality/annual-trend`
- `fetchQualityParetoRft()` → `GET /quality/pareto/rft`
- `fetchQualityParetoInspection()` → `GET /quality/pareto/inspection`

Each uses `fetch()` with `credentials: 'include'`, CSRF token from cookie, and handles 401/403.

### 2. Rewrite `quality.tsx` to Fetch from Backend
**File:** `resources/js/pages/quality.tsx` (REWRITE)

Replace mock data imports with live API calls using `useEffect` + `useState`:

**Section A — KPI Cards (2 rows of 4):**
- Row 1: BR CGL (F-REQ-101, B-02 grey), BR GTD Jour (F-REQ-102, B-02 grey), RFT Jour (F-REQ-104), BR Bundling Jour (F-REQ-106, B-01 grey if inactive)
- Row 2: BR GTD DDA (F-REQ-103, B-02 grey), RFT Année (F-REQ-105), BR Bundling Année (F-REQ-107, B-01 grey), BR Print (F-REQ-108, grey placeholder)

Status logic from backend: `value=null, status='pending'` → grey "En attente API DIVA"; `status='inactive'` → grey "Activation requise (B-01)"; `status='green'/'orange'/'red'` → normal display.

**Section B — BR Bar Chart + Defect Chart + Alerts:**
- BR par Étape: from `fetchQualityBrChart()` — bar chart with color-coded bars per chain, reference line at y=5
- Défauts par Opération: from `fetchQualityDefectChart()` — bar chart top 8 by OpNo
- Alert List: from `fetchQualityAlerts()` or generated from KPI values client-side

**Section C — QpTeamPodiums (F-REQ-118/119):**
- From `fetchQualityQpTeams()` — two side-by-side `QpTeamPodium` (best + worst)
- Shows partial scores with footnote "Score partiel — données DIVA en attente"

**Section D — Annual Trend Line Chart:**
- From `fetchQualityAnnualTrend()` — RFT & BR trend lines with reference lines

**Section E — Pareto Tabs:**
- Tab 1: Pareto RFT from `fetchQualityParetoRft()`
- Tab 2: Pareto Inspection from `fetchQualityParetoInspection()`
- Add cumulative % line (Pareto chart with bars + line)

**Section F — KPI Summary Table** (keep existing)

**Auto-refresh:** Use `setInterval` (60s default) to re-fetch all quality data. Update `LiveSyncPill` state.

**Loading/Error states:** Show skeletons on initial load, ErrorBanner on failure, per-section error isolation.

### 3. Implement AlertService on Backend
**File:** `app/Services/AlertService.php` (UPDATE)

Replace placeholder with real alert generation:
- Query latest RFT from `pieces_ok_jour` / `pieces_produites_jour`
- Query latest BR from `check_pass_qte`
- Generate alerts based on thresholds:
  - RFT < 95% → CRITICAL
  - RFT 95–98% → WARNING
  - BR > 5% per chain → CRITICAL
  - No alerts → "Tous les KPIs OK"

### 4. Add Sync Config Panel to Admin Page
**File:** `resources/js/pages/admin.tsx` (UPDATE)

Add a new `Panel` section for "Configuration Sync Backend":
- Fetch current intervals: `GET /admin/sync-config`
- Display inputs for: `quality_interval_seconds`, `production_interval_seconds`, `logistics_interval_seconds`
- Save: `PUT /admin/sync-config/{key}` with `{ value: N }`
- Show last updated timestamp per setting
- Validate min 60s, max 3600s

**File:** `resources/js/services/adminApi.ts` (UPDATE)

Add:
- `fetchSyncConfig()` → `GET /admin/sync-config`
- `updateSyncConfig(key, value)` → `PUT /admin/sync-config/{key}`

### 5. Backend: Verify AdminController Sync Config Endpoints
**File:** `app/Http/Controllers/Api/AdminController.php` (CHECK/UPDATE)

Verify `getSyncConfig()` and `updateSyncConfig()` methods exist and work:
- `getSyncConfig`: returns all `SyncSetting` records
- `updateSyncConfig`: validates value (60–3600), updates DB, busts cache, logs audit trail

### 6. Backend: Verify Sync Commands
**Files:** `app/Console/Commands/SyncNovacityQuality.php` (CHECK)

Verify `sync:quality` command exists and calls `SyncService::syncQuality()`.

---

## Files Modified
| File | Action |
|---|---|
| `resources/js/services/qualityApi.ts` | CREATE — new backend-facing quality API |
| `resources/js/pages/quality.tsx` | REWRITE — fetch from backend, 8 KPI cards, charts, podiums |
| `resources/js/services/adminApi.ts` | UPDATE — add sync config fetch/update |
| `resources/js/pages/admin.tsx` | UPDATE — add sync config panel |
| `app/Services/AlertService.php` | UPDATE — real alert generation |

## Files Verified (no changes needed)
| File | Status |
|---|---|
| `app/Http/Controllers/Api/QualityController.php` | ✅ Complete |
| `routes/web.php` | ✅ Quality routes defined |
| `routes/console.php` | ✅ Scheduler configured with DB intervals |
| `app/Models/SyncSetting.php` | ✅ Model with cache |
| `app/Console/Commands/SyncNovacityQuality.php` | ✅ Command exists |
| `resources/js/components/QpTeamPodium.tsx` | ✅ Component exists |

---

## Verification
1. **Quality page loads**: Navigate to `/quality` → see loading skeletons → data appears
2. **KPI cards**: RFT Jour/Année show live values; DIVA/Bundling cards show grey placeholders
3. **Charts render**: BR bar chart, defect chart, annual trend, both Pareto tabs
4. **QP Podiums**: Best + Worst podiums visible with partial scores
5. **Alerts**: Auto-generated based on KPI values
6. **Auto-refresh**: Wait 60s → data refreshes, LiveSyncPill updates
7. **Error handling**: Kill backend → ErrorBanner shows, no crash
8. **Admin sync config**: Navigate to `/admin` → see sync config panel → change interval → save → backend picks up
9. **Cron job**: Verify scheduler runs `sync:quality` at configured interval
