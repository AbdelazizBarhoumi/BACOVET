# Production Page API Audit — Implementation Plan

## Goal
Audit every card/modal on the production page to ensure all API-exposed fields are displayed, and sections hide gracefully when data is missing/null.

## Reference File
Create `PRODUCTION_API_AUDIT.md` documenting every API field vs. what's displayed per card.

---

## Card-by-Card Audit & Fixes

### Card 1: Chain Info Cards (Row 1)
**API exposes:** `id, of, article, designation, sam, sot, effectif, objectif, eff, hp, hs, wip, status, br_gtd, bpd, epd, ehd, entree_jour, sortie_jour`

**Issues:**
1. `status` — exposed but NOT displayed → Add colored status dot
2. `br_gtd` — exposed but NOT displayed → Add as small indicator next to eff
3. `entree_jour` — exposed but NOT displayed, also MISSING from TypeScript type → Add to type + display
4. `sortie_jour` — exposed but NOT displayed, also MISSING from TypeScript type → Add to type + display

**Files to modify:**
- `resources/js/services/productionApi.ts` — Add `entree_jour?: number` and `sortie_jour?: number` to `ChainInfo` type
- `resources/js/pages/production.tsx` — Display `status`, `br_gtd`, `entree_jour`, `sortie_jour` in chain card

---

### Card 2: KPI Cards (Row 2)
**API exposes:** `avg_efficience, avg_owe, rft_production, total_wip, total_lost_time, br_gtd, br_bundling, br_print`

**Issues:**
1. `total_wip` — has a KPI in the API response but NO card rendered → Add `ProductionKpiCard` for `total_wip`

**Files to modify:**
- `resources/js/pages/production.tsx` — Add `total_wip` card after `rft_production`

---

### Card 3: Efficience Gauges + WIP Gauges (Row 3)
**API exposes (WIP gauges):** `chaine, wip, raw_wip, target`

**Issues:**
1. `raw_wip` — exposed but NOT displayed → Show as label under gauge
2. `target` — exposed but NOT displayed → Show as label under gauge

**Files to modify:**
- `resources/js/pages/production.tsx` — Display `raw_wip` and `target` under each WIP gauge

---

### Card 4: Stoppage Timeline (Row 3)
**API exposes:** `chaine, motif, duration, start`

**Issues:**
1. Chain list is hardcoded as `['CH1', 'CH2', 'CH3', 'CH4']` → Derive from `stoppages` data dynamically

**Files to modify:**
- `resources/js/pages/production.tsx` — Replace hardcoded chain list with dynamic unique chains from data

---

### Card 5: Workshop Specific Panels (Row 4) — Couverture
No issues found. All fields used correctly.

---

### Card 6: OF Progress Donuts (Row 5)
**API exposes:** `of, pct, statut`

No issues found. All fields used.

---

### Card 7: SO Progress (Row 5)
**API exposes:** `chaine, realise, restant`

No issues found. All fields used.

---

### Card 8: Departage / Vignettes (Row 6 — Confection only)
**API exposes:** `employe, eff`

No issues found. All fields used.

---

### Card 9: Top Operators (Row 6)
**API exposes:** `nom, eff, min_std, min_pres`

**Issues:**
1. `min_std` and `min_pres` — exposed but only used in `allOps` chart, NOT in the `topOps` chart → These are optional and correctly only used in the composed chart

No fix needed.

---

### Card 10: WIP Area Chart (Row 6)
**API exposes:** `date, sortie, engagement`

No issues found. All fields used.

---

### Card 11: All Operators Composed Chart (Row 6)
**API exposes:** `nom, eff, min_std, min_pres`

No issues found. All fields used.

---

### Card 12: Efficience Trend (Row 7)
**API exposes:** `jour, eff`

No issues found. All fields used.

---

### Card 13: Coupe Tagging Table
**API exposes:** `id, date, chaine, shift, tag_theorique, tag_reel, ecart_pct, synced_at`

**Issues:**
1. `shift` — exposed but NOT displayed → Add column
2. `date` — exposed but NOT displayed → Could add as header/note (optional, low priority)

**Files to modify:**
- `resources/js/pages/production.tsx` — Add `shift` column to tagging table

---

### Card 14: Coupe OFs Table
**API exposes:** `id, of_number, article, designation, quantite, dt_debut, dt_fin, statut, created_at, updated_at, synced_at`

**Issues:**
1. `designation` — exposed but NOT displayed → Add column or tooltip
2. `statut` — exposed but NOT displayed → Add status column

**Files to modify:**
- `resources/js/pages/production.tsx` — Add `designation` and `statut` to OF table

---

### Card 15: Coupe QTE Departage Table
**API exposes:** `of, article, quantite`

No issues found. All fields used.

---

### Card 16: Inline vs Endline Chart
**API exposes (raw rows):** `id, log_date, shift_code, shortname, opera, count, synced_at`

**Issues:**
1. **DATA FORMAT MISMATCH** — API returns individual rows with `opera: "inline"/"endline"` and `count`, but the BarChart uses `dataKey="inline"` and `dataKey="endline"`. The data needs to be grouped by `shortname` (chaine) into `{ chaine, inline, endline }` format.
2. `shift_code` — exposed but NOT used
3. `log_date` — exposed but NOT used

**Files to modify:**
- `resources/js/pages/production.tsx` — Transform raw API data into grouped format before rendering

---

### Card 17: Serigraphie Coverage
**API exposes:** `value, status, target`

No issues found. All fields used.

---

### Card 18: Serigraphie Flux
**API exposes:** `article, couleur, entree, sortie`

**Issues:**
1. `couleur` — exposed but NOT displayed → Could add as tooltip or label

**Files to modify:**
- `resources/js/pages/production.tsx` — Add `couleur` info (tooltip on article)

---

### Card 19: Serigraphie Rejets Table
**API exposes:** `id, id_colis, reference, motif, qtte, date_rejet, synced_at`

**Issues:**
1. `reference` — exposed but NOT displayed → Add column
2. `date_rejet` — exposed but NOT displayed → Add column

**Files to modify:**
- `resources/js/pages/production.tsx` — Add `reference` and `date_rejet` columns

---

### Card 20: Modal (ProductionKpiDetailModal)
**Issues:**
1. The `total_wip` key is not in the `kpiMap` mapping → Add mapping
2. The `rft_production` key is not in the `kpiMap` mapping → Add mapping

**Files to modify:**
- `resources/js/components/production/ProductionKpiDetailModal.tsx` — Add missing KPI mappings

---

## Null/Empty Safety Fixes (all in production.tsx)

1. Chain info section: Already has empty state ✓
2. KPI cards: Already handle null with `??` fallbacks ✓
3. Gauge sections: Already check `gauges.length > 0` ✓
4. Stoppage timeline: Already has empty state ✓
5. OF donuts: Already has empty state ✓
6. Coupe/Seri panels: Already have empty states ✓

**Additional safety needed:**
- WIP gauges section renders even when `wipGauges` is empty (shows "En attente") — OK, no change needed
- Stoppage panel renders even when stoppages array is empty — OK, has empty message

---

## Implementation Order

1. **Create reference file** `resources/js/components/production/PRODUCTION_API_AUDIT.md`
2. **Fix ChainInfo type** — add `entree_jour`, `sortie_jour`
3. **Chain Info Cards** — display `status`, `br_gtd`, `entree_jour`, `sortie_jour`
4. **Add total_wip KPI card**
5. **WIP Gauges** — show `raw_wip` and `target`
6. **Stoppage Timeline** — dynamic chain list
7. **Coupe Tagging** — add `shift` column
8. **Coupe OFs** — add `designation`, `statut` columns
9. **Inline vs Endline** — fix data transformation
10. **Sérigraphie Flux** — add `couleur` tooltip
11. **Sérigraphie Rejets** — add `reference`, `date_rejet` columns
12. **Modal** — add missing KPI mappings

## Verification

- Run `npm run lint` (or equivalent) to check for type errors
- Run `npm run build` to verify compilation
- Visual check: each card should show all its API fields
- Visual check: no broken/empty sections when data is null
