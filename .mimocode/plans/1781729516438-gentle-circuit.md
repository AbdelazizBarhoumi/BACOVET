# Plan: Apply Consistent Tooltip Styling to All Recharts Charts

## Problem
Only the quality page BR chart (quality.tsx:760) has full tooltip styling with `cursor`, `labelStyle`, and `itemStyle`. The other 19 Recharts `<Tooltip>` usages across 5 pages lack these enhanced props, making hover interactions look inconsistent and unstyled.

## Approach
Add `cursor`, `labelStyle`, and `itemStyle` props to every Recharts `<Tooltip>` across all page files. The container style (`contentStyle`) already comes from the shared `tt`/`tooltipStyle` variable (updated with borderRadius, boxShadow, padding, fontFamily).

## Files to Modify

### 1. `resources/js/pages/quality.tsx` (3 Tooltips to enhance)
- Line 925: `<Tooltip contentStyle={tooltipStyle} />` → add cursor, labelStyle, itemStyle
- Line 986: same
- Line 1042: same
- Line 760: already has full styling — no change needed

### 2. `resources/js/pages/development.tsx` (3 Tooltips)
- Line 315: `<Tooltip contentStyle={tooltipStyle} />` → add cursor, labelStyle, itemStyle
- Line 381: same
- Line 401: same

### 3. `resources/js/pages/methods.tsx` (1 Tooltip)
- Line 406: `<Tooltip contentStyle={tooltipStyle} formatter={...} />` → add cursor, labelStyle, itemStyle

### 4. `resources/js/pages/production.tsx` (10 Tooltips)
- Lines 761, 856, 897, 928, 964, 994, 1042, 1082, 1332, 1376: add cursor, labelStyle, itemStyle to each

### 5. `resources/js/pages/logistics.tsx` (2 Tooltips)
- Lines 449, 682: add cursor, labelStyle, itemStyle

## Props to Add to Each Tooltip
```tsx
cursor={{ fill: 'var(--muted)', opacity: 0.3 }}
labelStyle={{ color: 'var(--foreground)', fontWeight: 700, fontSize: 11, marginBottom: 4 }}
itemStyle={{ fontSize: 11 }}
```

## Verification
- Run `npx vite build` to confirm no compilation errors
- Visually check each page's charts show consistent tooltip styling on hover
