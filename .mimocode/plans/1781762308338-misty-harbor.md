# Production Page — All Changes (Session Complete)

## Files Modified

### 1. `app/Http/Controllers/Api/ProductionController.php`

**Removed `applyFilters` from global daily totals:**
- `pieces_ok_jour` (RFT Production) — global, not per-chain
- `pieces_produites_jour` (RFT Production) — global, not per-chain
- `rejets_inspection_paquet` (BR Bundling) — global, not per-chain

**Added 10 breakdown handlers in `breakdown()` method:**

| KPI | Spec | Handler |
|-----|------|---------|
| `owe_chaine` | F-REQ-204 | OWE per chain from efficience + GPRO SAM/SOT |
| `wip_chaine` | F-REQ-205 | WIP quantity per chain with status |
| `arrets_non_planifies` | F-REQ-207 | Timeline: chaine, motif, duration, minutes_perdues |
| `br_gtd` | F-REQ-102 | Defect % per chain from check_pass_qte |
| `br_bundling` | F-REQ-106 | Bundling reject rate |
| `br_print` | F-REQ-108 | Print reject rate |
| `rft_production` | F-REQ-104 | RFT global value |
| `taux_archivage` | F-REQ-216 | Archivage % per chain from suivi_paquets |
| `respect_temps_estime` | F-REQ-218 | Respect rate per group from cotation |
| `temps_acceptes` | F-REQ-219 | Accept rate per group from gammes |

---

### 2. `resources/js/components/production/productionKpiDetailConfig.ts`

- `br_gtd.id`: `F-REQ-107` → `F-REQ-102`
- `br_gtd.description`: Updated to CDC formula
- `br_gtd.source.system`: `'Quality'` → `'DIVA / GPRO'`
- `br_gtd.formula`: numerator/denominator clarified
- `br_bundling.description`: Updated to CDC formula
- `br_bundling.source.system`: `'Quality'` → `'GPRO Prod'`
- `br_print.description`: Updated to CDC formula
- Added `'computed'` to `formula.type` union
- Added `'blocked'` to `source.status` union

---

### 3. `resources/js/components/production/ProductionKpiDetailModal.tsx`

- Removed unused `trendData` prop from interface
- Dynamic modal header: "Série 100/200/300/350" based on F-REQ number
- Conditional Breakdown & Viz section (hidden when both `breakdownType` and `miniVizType` are `'none'`)
- Conditional breakdown table (hidden when `breakdownType === 'none'`)
- Conditional viz (hidden when `miniVizType === 'none'`)
- Conditional Source "Source:" line (hidden when both endpoints are null)
- Added `timeline` breakdown table type for F-REQ-207

---

### 4. `resources/js/components/widgets.tsx`

- Implemented BigNumberCard loading skeleton when `isLoading=true`
- Removed unused `error` prop from type

---

### 5. `resources/js/services/productionApi.ts`

- Removed unused `fetchProductionAlerts` export
- Cleaned up leftover `// ...` comments

---

## Verification

1. `php -l` — no PHP syntax errors
2. `npx tsc --noEmit` — no TS errors in production files
3. `npx vite build` — passes
4. Click each KPI card → breakdown tables populate with real data
5. Modal header shows correct series
6. No more N/A on RFT Production / BR Bundling cards (when data is synced)
