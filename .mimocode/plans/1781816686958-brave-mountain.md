# Remove outdated "Source manuelle" / "sync pending" banners

## Context

The Google Drive sync is already fully implemented and operational:
- `SyncService::syncGoogleDrive()` runs via `sync:drive` artisan command
- `sync_drive_*` tables are populated (cotation, gammes, development, br_print, etc.)
- Sync interval configured at 6h default

The remaining "manual source" / "connector needs activation" / "sync pending" notes are **stale** and should be removed before delivery.

## Changes (3 files)

### 1. `resources/js/components/development/DevKpiDetailModal.tsx` (lines 210-227)
**Update** the `{/* Google Drive notice */}` block. Keep the "Source Google Drive" header and sync frequency info, but remove the outdated "Le connecteur Google Sheets doit être activé côté Cloud" sentence. Updated text:
```
Ces KPIs sont alimentés via Google Sheets. La synchronisation est effectuée 4 fois par jour.
```

### 2. `app/Http/Controllers/Api/ProductionController.php` (line 1191)
**Remove** the `metadata` key from the `serigraphieRejets` response:
```php
// Before:
return response()->json(['data' => $query->get(), 'metadata' => ['br_print_note' => 'F-REQ-108: BR Print (Google Drive) sync pending.']]);

// After:
return response()->json(['data' => $query->get()]);
```

### 3. `resources/js/pages/production.tsx` (lines 1530-1535)
**Remove** the `br_print_note` display block — the backend will no longer send it, so this dead code can go.

### 4. `resources/js/components/development/DEVELOPMENT_SECTIONS_REFERENCE.md` (line 427)
**Update** the "Source Manuelle Banner" section to reflect that the banner has been removed. The data flow description is still accurate and can stay.

## Files NOT changed
- `source="Google Drive"` props on KpiCards in quality.tsx / development.tsx — these are **accurate source labels**, not warnings. Google Drive IS the source; they stay.
- `specs.md`, `OldSprints.md`, `Sprints.md` — historical docs, not user-facing.

## Verification
1. `npx tsc --noEmit` — no type errors
2. `php artisan test` — backend tests pass
3. Visual check: `/development` page modal should no longer show the Google Drive connector warning
4. Visual check: `/production` Sérigraphie tab should no longer show "sync pending" note
