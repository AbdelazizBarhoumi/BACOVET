# Plan: Fix field_codes and endpoints in Production & Logistics configs to match prod.json / logistics.json

## Goal
Ensure every `field` value in formula numerators/denominators and every `novacityEndpoint` matches the actual `field_code` and `api_endpoint` from prod.json and logistics.json.

---

## Part 1: Production Config Fixes (`productionKpiDetailConfig.ts`)

### 1. `efficience_chaine` — formula fields don't match prod.json
- prod.json says: `quantite` from `/api/data/q/qte_produite` + `minutes_presence` from `/api/data/q/minutes_presence`
- Current: `qty_declared_x_sam` / `headcount_x_minutes`
- **Fix**: numerator field → `quantite`, denominator field → `minutes_presence`
- **Fix**: endpoint → `GET /api/data/q/qte_produite + GET /api/data/q/minutes_presence`

### 2. `efficience_cumulee` — field names
- prod.json: `minutes_produites` from `/api/data/q/minutes_produites`, `minutes_presence` from `/api/data/q/minutes_presence`
- Current: `total_minutes_produites` / `total_minutes_presence`
- **Fix**: numerator field → `minutes_produites`, denominator field → `minutes_presence`

### 3. `wip_chaine` — numerator field name wrong
- prod.json: `sortie_jour` from `/api/data/q/wip_chaine`
- Current: `sortie_post93`
- **Fix**: numerator field → `sortie_jour`
- **Fix**: Also add `en_cours` field reference (prod.json id 20: `en_cours` from `/api/data/q/wip_chaine`)

### 4. `taux_fiabilite_systeme` — field_b wrong
- prod.json: tag_reel from `/api/data/q/taging_reel`, sortie_jour from `/api/data/q/wip_chaine`
- Current: field_b = `tag_theorique`
- **Fix**: field_b → `sortie_jour`

### 5. `taux_archivage` — fields don't match prod.json
- prod.json: OF archivés = `OFabrication` from `/api/data/ofabrication`, OF soldés = `of (statut=termine)` from `/api/data/q/etat_avancement`
- Current: `est_archive` / `est_solde` (wrong)
- **Fix**: numerator field → `OFabrication`, denominator field → `of (statut=termine)`
- **Fix**: endpoint → `GET /api/data/ofabrication + GET /api/data/q/etat_avancement`

### 6. `respect_temps_estime` — missing heures_prod reference
- prod.json: Temps production = `heures_prod` from `/api/data/q/efficience_chaine`
- Current: `respect_count` / `total_count` (computed, no field refs)
- **Fix**: Add field references: numerator → `respect_count` (computed), denominator → `heures_prod` from `/api/data/q/efficience_chaine`

### 7. `top_operateurs` — field name
- prod.json: `quantite` from `/api/data/q/qte_produite_indiv_jour`, `temps_reel_s` from `/api/data/q/temps_operation`, `minutes_presence` from `/api/data/q/minutes_presence`
- Current: `qte_x_temps_op` (computed) — OK as computed, but endpoint should list all 3
- **Fix**: endpoint → `GET /api/data/q/qte_produite_indiv_jour + GET /api/data/q/temps_operation + GET /api/data/q/minutes_presence` ✓ (already correct)

### 8. `code_article` — field_code
- prod.json: `IDOFabrication` from `/api/data/ofabrication`
- Current: endpoint `GET /api/data/ofabrication` ✓
- **Fix**: Add export field `IDOFabrication`

### 9. `designation_article` — endpoint
- prod.json: `Description` from `/api/data/mp`
- Current: endpoint `GET /api/data/mp` ✓
- **Fix**: Add export field `Description`

### 10. `arrets_non_planifies` — missing motif field
- prod.json: motif = `LostTypeDesc` from `/api/data/losttype`
- Current: no motif endpoint reference
- **Fix**: Add note about LostType lookup from `/api/data/losttype`

---

## Part 2: Logistics Config Fixes (`logistics/kpiDetailConfig.ts`)

### 1. `dot` — numerator field wrong
- logistics.json: QT livrée on-time = `NbOF_Livres_Total` from `/api/data/q/nombre_d_ofs_livres_avec_date_de_transfert_coupe_coupe_j`
- Current: `Non mappé`
- **Fix**: numerator field → `NbOF_Livres_Total`
- **Fix**: endpoint → `GET /api/data/q/nombre_d_ofs_livres_avec_date_de_transfert_coupe_coupe_jemmel`

### 2. `lead_time` — numerator field wrong
- logistics.json: Lead time = `MoyenneJours` from `/api/data/q/moyenne_date_de_transfert_date_de_reservation`
- Current: `Non mappé`
- **Fix**: numerator field → `MoyenneJours`
- **Fix**: endpoint → `GET /api/data/q/moyenne_date_de_transfert_date_de_reservation`

### 3. All other logistics KPIs — verified ✓
- DOT denominator: `quantite_engagee` from `/api/data/q/qte_engagement` ✓
- HOT: `quantite_engagee` ✓
- Respect plan: `quantite_realisee` from `/api/data/q/etat_avancement` ✓
- Stock mort: `Qtte_SansMvt_365j` / `Quantite_Totale_Stock` ✓
- Occupation: `NbRouleaux` / `Total_Conteneurs` ✓
- Livraison: `OF_AvecTransfertCoupe_Total` ✓
- Délai: `MoyenneJours` ✓
- Stock typologie/provenance/brand: `Quantite` ✓

---

## Files to modify
1. `resources/js/components/production/productionKpiDetailConfig.ts` — fix field_codes and endpoints
2. `resources/js/components/logistics/kpiDetailConfig.ts` — fix DOT numerator and lead_time numerator

## Verification
1. `npx vite build` — zero errors
2. Cross-check each formula field against its JSON file
