# Data Page — Performance, Style & UX Improvements

**File to modify:** `resources/js/routes-v1/pages/data.tsx` (1583 lines)
**Also touch:** `resources/css/app.css` (for shared select arrow utility)

---

## Changes (based on current file state)

### 1. Text Visibility
- Line 100: `truncate max-w-[160px]` → `min-w-0 max-w-full break-all`
- Line 104: `truncate` → `break-all`
- Line 1378: `max-w-[180px]` → `min-w-[120px]`
- Line 1469: `max-w-[180px]` → `min-w-[120px]`
- Line 1444: `max-w-[280px]` → `min-w-[120px]`
- Line 1540: `max-w-[280px]` → `min-w-[120px]`
- Add `title` to KPI input (line 1093), Variable input (line 1146), Name input (line 1140)

### 2. Dropdown Styling
- Replace `selectBase` (lines 51-55) with custom chevron via `appearance-none` + SVG data URI
- Add `formulaSelectBase` for FormulaBuilder selects (lines 370, 381, 402, 410)
- Improve disabled state with dashed border

### 3. Formula Cell Restyle
- Restructure container with header bar, dividers, pill chips, styled operators, dashed add buttons, circular remove button, prominent result

### 4. Performance
- Add `fetchInBatches` helper, batch preview/test fetches (8 at a time)
- Add `content-visibility: auto` to rows, `contain: layout style` on scroll container
- Remove `transition-colors duration-150` from `<tr>`

### 5. NestedKeySelector styling
- Apply `selectBase` to nested selects (already using it at lines 505, 533)
