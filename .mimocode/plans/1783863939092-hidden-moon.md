# Fix: Exec/Test Live returns "Unauthenticated"

## Problem
`/api/novacity/{path}` proxy is inside `Route::middleware(['auth', 'active.user'])` which requires Laravel web auth. The data page uses `data_users` guard.

## Fix
Move the novacity proxy route outside the auth middleware group (same as settings and data-mappings).
