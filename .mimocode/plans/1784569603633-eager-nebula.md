# Plan: Fix All TypeScript Errors

Run `npx tsc --noEmit` to find all type errors, then fix each one.

Known errors from previous runs:
- `data.tsx:2257` — `cible_operator` type mismatch (pre-existing)

New files that may have errors:
- `kpiFilterEngine.ts` — new computed_result fast path
- `v2ProductionApi.ts` — new computed_result type
- `useV2KpiCalc.ts` — computed_result passthrough

## Steps

1. Run `npx tsc --noEmit --pretty` to get full error list
2. Fix each error in the affected file
3. Re-run until clean (only pre-existing errors remain)
