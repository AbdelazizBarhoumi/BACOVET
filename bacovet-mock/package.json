{
  "name": "bacovet-mock-api",
  "version": "1.0.0",
  "description": "Fake API server driven by storage/app/public/data.json — endpoints, shapes and generated rows all derive from that file. Runs on port 3005.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
